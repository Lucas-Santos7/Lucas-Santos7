nome: GitHub-Profile-3D-Contrib

em:
  horário: # 03:00 JST == 18:00 UTC
    - cron: "0 18 * * *"
  workflow_dispatch:

Empregos:
  Construir:
    é executado: ubuntu-mais recente
    nome: generate-github-profile-3d-contrib
    Passos:
      - Usos: Ações/checkout@v2
      - Usos: yoshi389111/github-profile-3d-contrib@0.6.0
        env:
          GITHUB_TOKEN: ${{ segredos. GITHUB_TOKEN }}
          NOME DE USUÁRIO: ${{ github.repository_owner }}
      - nome: Commit & Push
        executar: |
 configuração do git user.name github-actions
 user.email github-actions@github.com de configuração do git
          git add -A .
 git commit -m "gerado"
 vá empurrar
